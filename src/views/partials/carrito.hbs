<h1>este es el carrito </h1>
<div>

    <table class="table">
  <thead>
    <tr>
      <th scope="col">carrito ID = </th>
      <th scope="col">productos = </th>


    </tr>
  </thead>
  <tbody>

    {{#each carrito}}
        <tr>
            <th scope="row">{{this.id}}</th>
            <th scope="row">{{this.productos}}</th>
        </tr>
    {{/each}}
  </tbody>
</table>

<button class="btn btn-primary" onclick="location.href='/enviarMail'" >finalizar compra</button>

</div>